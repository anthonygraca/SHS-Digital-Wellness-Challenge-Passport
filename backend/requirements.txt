# Pinned runtime dependencies for the container image.
#
# pyproject.toml stays the source of truth for *which* packages the app needs and
# the minimum version of each; this file pins the exact resolved set so an image
# built today and one built next month are byte-identical. Dev/test extras are
# deliberately absent — they are not installed in the runtime image.
#
# Regenerate after changing pyproject's [project.dependencies]:
#
#   docker run --rm python:3.12-slim sh -c \
#     "pip install -q fastapi 'uvicorn[standard]' sqlalchemy pydantic-settings \
#      pyjwt python-multipart && pip freeze" > backend/requirements.txt
#
# (then re-add this header)

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
click==8.4.2
fastapi==0.139.0
greenlet==3.5.3
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
PyJWT==2.13.0
python-dotenv==1.2.2
python-multipart==0.0.32
PyYAML==6.0.3
SQLAlchemy==2.0.51
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1
